export enum NPM_TAG {
  LATEST = 'latest',
  BETA = 'beta',
}

export enum NPM_BUMP_TYPE {
  MAJOR = 'major',
  MINOR = 'minor',
  PATCH = 'patch',
}

export enum PUBLISH_STATUS {
  PENDING = 'pending',
  SUCCESS = 'success',
  FAILED = 'failed',
}
